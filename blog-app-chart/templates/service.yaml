apiVersion: v1
kind: Service
metadata:
  name: {{ include "blog-app-chart.fullname" . }}
  labels:
    {{- include "blog-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blog-app-chart.selectorLabels" . | nindent 4 }}
